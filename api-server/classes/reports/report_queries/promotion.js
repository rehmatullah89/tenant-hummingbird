
let promotion_id = "promotions.id";
//let company_id = "promotions.company_id";

module.export = {
    promotion_id: promotion_id,
   //company_id:   company_id,

    //promotion_name:    " (SELECT name FROM promotions WHERE id = " + this.promotion_id + ") ",
    //promotion_amount:  " (SELECT value FROM promotions WHERE id = " + this.promotion_id + ") "

}