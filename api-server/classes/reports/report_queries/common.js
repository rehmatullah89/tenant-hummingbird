class LeadQueries {
  constructor(data, date) {
    
    this.queries = {
      common_id:                        `(select "")`,
      common_manager_notes:             `(select "")`,
    }
  }
}

module.exports = LeadQueries;
